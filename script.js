const desktop=document.getElementById('desktop');let topZ=40;const experience={splash:{badge:'COMMUNITY · 2025—NOW',title:'Splash Foundation',text:'As Head of Marketing and a swimming coach, I help migrant domestic workers build confidence in the water while helping more people discover the program.',points:['Program reach increased by approximately 120%','Adapted coaching to different comfort levels','Leadership through listening and respect']},digital:{badge:'BUILDING · 2022—NOW',title:'Digital Studio',text:'I coordinate remote collaborators and turn user feedback into product decisions, working across scripting, design, planning, funding, and communication.',points:['Python · Java · Lua','Project roadmaps and team coordination','Learning from testing, failure, and iteration']},reading:{badge:'EDUCATION · 2023—2025',title:'Youth Reading Coach',text:'I designed engaging reading and grammar sessions for younger students at NLCS Singapore, focusing on confidence as much as accuracy.',points:['Patient, individual support','Clear and encouraging explanations','Consistent community service']}};function renderExperience(key){const d=experience[key];document.getElementById('experienceContent').innerHTML=`<span class="file-badge">${d.badge}</span><h2>${d.title}</h2><p>${d.text}</p><ul>${d.points.map(x=>`<li>→ ${x}</li>`).join('')}</ul>`}renderExperience('splash');document.querySelectorAll('.file-tab').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.file-tab').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');renderExperience(b.dataset.tab)}));function openWindow(id){const w=document.getElementById(id);if(!w)return;w.classList.add('active');w.style.zIndex=++topZ}function closeWindow(w){w.classList.remove('active')}document.querySelectorAll('[data-open]').forEach(el=>el.addEventListener('click',()=>openWindow(el.dataset.open)));document.querySelectorAll('.close').forEach(el=>el.addEventListener('click',e=>closeWindow(e.target.closest('.window'))));document.querySelectorAll('.close-window').forEach(el=>el.addEventListener('click',e=>closeWindow(e.target.closest('.window'))));document.querySelectorAll('.window').forEach(w=>{w.addEventListener('pointerdown',()=>w.style.zIndex=++topZ);const bar=w.querySelector('.window-bar');let drag=null;bar.addEventListener('pointerdown',e=>{if(e.target.classList.contains('close')||innerWidth<721)return;const r=w.getBoundingClientRect();drag={dx:e.clientX-r.left,dy:e.clientY-r.top};w.classList.add('dragging');bar.setPointerCapture(e.pointerId)});bar.addEventListener('pointermove',e=>{if(!drag)return;const x=Math.max(0,Math.min(innerWidth-w.offsetWidth,e.clientX-drag.dx));const y=Math.max(48,Math.min(innerHeight-w.offsetHeight-80,e.clientY-drag.dy));w.style.left=x+'px';w.style.top=y+'px';w.style.transform='none'});bar.addEventListener('pointerup',()=>{drag=null;w.classList.remove('dragging')})});function tick(){document.getElementById('clock').textContent=new Intl.DateTimeFormat('en-GB',{weekday:'short',hour:'2-digit',minute:'2-digit'}).format(new Date())}tick();setInterval(tick,1000);document.getElementById('themeToggle').addEventListener('click',()=>desktop.classList.toggle('alt'));document.addEventListener('keydown',e=>{if(e.key==='Escape'){const a=[...document.querySelectorAll('.window.active')].sort((x,y)=>(+y.style.zIndex||0)-(+x.style.zIndex||0))[0];if(a)closeWindow(a)}});
